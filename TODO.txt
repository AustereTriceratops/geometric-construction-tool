#== UI/UX
+ (X) outline buttons to denote the active input mode
+ (X) side-buttons to display the state of the straightedge and compass modes
    + (X) straightedge
    + (X) compass
+ (X) tags that show the name of the tool upon mouseover
+ (X) click+dragging the canvas around
+ (X) zoom in and out of the canvas
+ (X) change cursor when drawing
+ (X) dotted straightedge line should extend beyond the anchoring points
+ Add a mark on the preview line/arc to denote where the user would start drawing
+ (X) hollow cricle or crosshair follows pointer while placing straightedge/compass points?
+ (X) right-click drag to pan canvas when drawing
+ (X) while drawing with the compass and straightedge tools, allow user to click a point to reset with new anchor point
+ info icon in corner for controls
+ (X) right-click to reset compass and straightedge while choosing secondary point


#== features
+ straightedge
    + (X) dotted preview line
    + (X) state machine:
        + (X) select first point
        + (X) select second point
        + (X) the user can then draw along
+ compass
    + (X) dotted preview circle
    + state machine:
        + (X) select first point
        + (X) either point or background canvas must be selected next
        + (X) the user can then draw along
        + user can "unset" the anchor point while keeping the length of the compass fixed
+ colliders for lines
+ colliders for arcs
+ have compass and straightedge tools snap to nearby points

#== Codebase
+ (X) data classes for Points, Lines, Arcs
    + (X) Points converted to Vector2
    + (X) Lines to [Vector2, Vector2]
    + (X) Arcs to {center, radius, startAngle, endAngle}
    + (X) Add a dedicated Point dataclass 
+ (X) move sidebar to separate component
+ (X) move sidebar buttons to separate component
+ (X) edit history for lines
+ (X) edit history for arcs
+ (X) set up ortho camera for tighter control of rendering
+ (X) create a plane mesh to act as the background
+ code for finding intersections between lines and arcs (any combination)
+ (X) move entire canvas out of _app and into a separate scene component
+ 'fuse' together overlapping lines or arcs
+ (X) track "winding number" for arcs
+ gather exports together
+ (X) change cameraOffset to vector2
+ (X) set up unit testing
+ docstrings
+ highlighted objects should be stored in the app state

#== Bugs
+ (X) Dragging results in placing a point when the mouse comes up
+ (X) Should hide secondary input state buttons when tool is not selected
+ (X) hitting "undo" when multiple lines and arcs are present erases all of them (during drawing phase)
+ (X) crash when place a new point -> use compass/straightedge/click undo button during ONE_SEL